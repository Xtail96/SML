#include "sensor.h"
Sensor::Sensor()
{

}

void Sensor::update()
{
    /*if(isEnabled)
    {
        if(isActive == buffer)
        {
            isStateChanged = false;
        }
        else
        {
            isActive = buffer;
            isStateChanged = true;
        }

        return isStateChanged;
    }
    else
    {
        return 0;
    }*/
}
